@using Modelo.Entidades

@model Solicitud_Det

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Borrar Capacidad Pago</h1>

<div class="container">
    <div class="row">
        <div class="">
            <h3 class="text-center">Eliminar el Detalle Solicitud del solicitante : @Model.Solicitud.Afiliado.Solicitante.Nombres @Model.Solicitud.Afiliado.Solicitante.Apellidos</h3>
        </div>
    </div>
    <div class="row">
        <div class="">
            <form method="post" asp-action="Delete">
                <input type="hidden" asp-for="Solicitud_DetId" />
                <div class="form-group">
                    <label class="control-label">Rol de Pagos</label>
                    <select class="form-control" asp-for="solicitudId" asp-items="@ViewBag.selectListSolicitud" disabled>
                        <option value="">--Selecciona--</option>
                    </select>
                </div>
                <div class="form-group">
                    <label class="control-label">Declracion Impuestos</label>
                    <select class="form-control" asp-for="amortizacionId" asp-items="@ViewBag.selectListAmortizacion" disabled>
                        <option value="">--Selecciona--</option>
                    </select>
                </div>
                <div class="form-group">
                    <input class="btn btn-success" type="submit" value="Eliminar" />
                </div>
            </form>
        </div>
    </div>
</div>
